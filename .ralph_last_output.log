Done. Here's a summary of what was completed:

**Fixed and committed:** The `perched_lateral_flow!` and `subsurface_lateral_flow!` functions in `src/biogeophys/soil_hydrology.jl`.

**Bug fixed:** BoundsError when accessing `col_zi[c, k-1]` with `k=1`. In Fortran, `zi(c, 0) = 0.0` represents the ground surface, but Julia's 1-based indexing makes index 0 invalid. Fixed by using `k > 1 ? col_zi[c, k-1] : 0.0` at three locations:
- Lines 1627, 1634 in `perched_lateral_flow!` (frost table and perched water table layer location loops)
- Line 2127 in `subsurface_lateral_flow!` (rising water table loop)

**Test results:** All 7818 tests pass (4 new tests added for the two functions).
