# CLM.jl GPU Kernelization — Remaining Work Inventory & Plan

Status as of 2026-06-01. Goal: full `clm_drv!` timestep on GPU + AD-on-GPU.

**Done (whole-function GPU, Metal-validated at parity):** `soil_temperature!`,
`soil_water!` (ZD09 + moisture_form), `lake_temperature!`, `canopy_fluxes!`,
`photosynthesis!` (A1), `bareground_fluxes!` (A2), `lake_fluxes!` (A3),
`soil_fluxes!` (A4), `surface_radiation!` + `canopy_sun_shade_fracs!` (A5),
`canopy_interception_and_throughfall!` (A6), and the **A8 hydrology cluster**:
`water_table!`, `set_soil_water_fractions!`, `set_floodc!`, `set_qflx_inputs!`,
`route_infiltration_excess!`, `infiltration!`, `total_surface_runoff!`,
`renew_condensation!`, `saturated_excess_runoff!`, `infiltration_excess_runoff!`,
`compute_effec_rootfrac_and_vert_tran_sink!`, and the **A7 snow suite**:
`combine_snow_layers!`, `divide_snow_layers!`, `snow_compaction!`, the
snow-percolation chain (`bulk_flux_snow_percolation!`, `update_state_snow_percolation!`,
`calc_and_apply_aerosol_fluxes!`, `post_percolation_adjust_layer_thicknesses!`,
`sum_flux_add_snow_percolation!`), `build_snow_filter!`, the 5 snow-capping fns,
and `handle_new_snow!`'s inline loops.
Plus ~156 element-wise ops, both batched linear solvers, and the
friction-velocity / photosynthesis-Ci-core kernels.

Established patterns (reuse these): per-(c,j) element-wise kernels with
eltype-converted literals + String→Int config; grouped device-view
`@adapt_structure` structs for field-heavy loops (alias struct fields to locals
so the body stays verbatim; **Metal caps total kernel args ~31** — bundle scalar
constants into one isbits struct, give Float64-kwarg fields their own type param
for AD); `_scatter_add!` (atomic on GPU, `+=` on Dual/CPU) for patch→column;
per-column kernels with internal sequential level/substep loops for loop-carried
deps; mask/`active`-based iteration instead of host filter compaction.

Per-function gate: full suite 15681/15681 + `--check-bounds=yes` + a
`scripts/gpu_validate_<fn>_e2e.jl` Metal harness at 0.0/round-off parity.

---

## Phase A — Biogeophys driver completion ("the other half" of clm_drv!)

Ordered by value × reuse-of-existing-patterns (do top-down):

| # | Target | Status now | Pattern / difficulty | Notes |
|---|--------|-----------|----------------------|-------|
| A1 | **photosynthesis! Ci solver** | ✅ DONE (Metal whole-fn, 14 fields 0.0) | per-patch + per-canopy-level loops; Ci Brent/hybrid core already GPU-callable | Completes the canopy chain (item #2). Self-contained. |
| A2 | **bareground_fluxes!** | ✅ DONE (Metal whole-fn, 24/25 fields 0.0, kbm1 6.6e-08 F32) | stability iteration — SAME shape as canopy_fluxes Newton (active-mask rework + eltype literals + device-view structs) | Also fixed a latent AD bug: grnd_ch4_cond_patch needed its own Vg type param (Float64 kwarg vs Dual state). |
| A3 | **lake_fluxes!** | ✅ DONE (Metal whole-fn, 19 outputs ≤3.7e-05) | per-column Newton-Raphson | mirrors lake_temperature approach; LakeFluxDV device-view struct. |
| A4 | **soil_fluxes!** | ✅ DONE (Metal whole-fn, 17 fields exact 0.0) | per-col/per-patch energy partitioning | feeds soil_temperature outputs; 3 device-view bundles + _scatter_add! errsoi average. |
| A5 | **surface_radiation! + canopy_sun_shade_fracs!** | ✅ DONE (Metal whole-fn, 43 fields exact 0.0) | per-patch/per-band/per-layer, branchy | radiation pair; 5 device-view bundles, both snow/no-snow + urban/veg/bare + near-local-noon branches exercised. |
| A6 | **canopy_interception_and_throughfall!** | ✅ DONE (Metal whole-fn, 15 fields, 12 at 0.0) | finished patch→column scatter (`_scatter_add!`) | scatter exercised by a 2-patch→1-col harness. |
| A7 | **Snow suite**: handle_new_snow!, build_snow_filter!, bulk_flux_snow_percolation!, update_state_snow_percolation!, calc_and_apply_aerosol_fluxes!, post_percolation_adjust_layer_thicknesses!, sum_flux_add_snow_percolation!, snow capping (5 fns), snow_compaction!, combine/divide_snow_layers! | ✅ DONE (all whole-fn on Metal at 0.0; re-validated post-merge across snl=0/-1/-2/-3) | variable-`snl` snow-layer loops, loop-carried across layers, per-species aerosol scatter | hardest mechanical cluster — done via per-column kernels w/ in-thread sequential layer restructuring (combine/divide mutate snl in padded arrays); 4 agents, disjoint line-regions. **Follow-up:** handle_new_snow! orchestrator still calls host loops in snow_cover_fraction.jl (bulkdiag_new_snow_diagnostics!, update_snow_depth_and_frac!) — finish in A10. |
| A8 | **Hydrology/runoff**: set_soil_water_fractions!, set_floodc!, saturated_excess_runoff!, set_qflx_inputs!, infiltration_excess_runoff!, route_infiltration_excess!, infiltration!, total_surface_runoff!, compute_effec_rootfrac_and_vert_tran_sink!, water_table!, renew_condensation! | ✅ DONE (all 11 whole-fn on Metal at 0.0/round-off; re-validated post-merge) | per-col + sequential water-table search | water_table! long pole done: 6 host loops (3 sequential nlevsoi searches w/ loop-carried state) → one per-column kernel, 2 device-view bundles. **Tail also DONE** (separate batch): drainage! + clm_vic_map!, perched_water_table! + theta_based_water_table!, perched/subsurface_lateral_flow!, update_urban_ponding! + irrigation withdrawals — all whole-fn on Metal at 0.0/round-off, validated post-merge. Still HOST: hillslope-connectivity path of lateral flow (kept on CPU; device asserts non-hillslope). |
| A9 | **Urban path**: urban_radiation!, urban_fluxes! | ✅ DONE (both whole-fn on Metal; re-validated post-merge) | per-landunit/per-patch urban canyon | urban_fluxes! iterative canyon-air solve done as ONE thread per urban landunit running the full 3-iter loop-carried stability solve in-thread + member-patch/column gather over contiguous coli:colf/patchi:patchf ranges — each landunit owns its accumulation, so NO cross-thread reduction needed. Day worst 1.3e-7, low-wind worst 1.8e-5 (F32). |
| A10 | **Smaller/init**: biogeophys_pre_flux_calcs!, calc_root_moist_stress!, calc_soilevap_resis!, calc_ozone_stress!, calculate_surface_humidity!, set_actual_roughness_lengths!, dust_dry_dep!, alt_calc!, update_daylength!, clm_drv_init! | ✅ DONE (all whole-fn on Metal at 0.0/round-off; re-validated post-merge via 11 e2e harnesses) | small per-col/per-patch loops | also kernelized the handle_new_snow! tail (bulkdiag_new_snow_diagnostics! + update_snow_depth_and_frac! + add_newsnow_to_intsnow! in snow_cover_fraction.jl), closing A7's follow-up. |

## Phase B — BGC / `use_cn` path (deferred biogeochemistry)

**Scope: ~562 host loops across 42 files (~25.5k LOC).** Far larger than Phase A.
**Module 1 of 7 (C/N state-update cascade, ~100 loops) is now COMPLETE** — every
state-update fn runs whole-function on Metal at parity. Remaining ~460 loops across
decomposition / methane / phenology / fire / N-cycling / tail. Sub-order by module:

| Module | Files | ~Loops | Kernelized | Priority/notes |
|--------|-------|--------|-----------|----------------|
| Carbon/Nitrogen state updates | 8 | ~100 | ✅ **DONE** — entire C/N state-update cascade whole-fn on Metal at 0.0/round-off parity | core cascade complete (element-wise, high reuse). Pattern-setter `c_state_update1!` done by hand; the rest done in one parallel workflow batch (7 disjoint-file agents + per-file review, cherry-picked onto main): `c_state_update0!`/`c_state_update_dyn_patch!`, `c_state_update2!/2h!/2g!`, `c_state_update3!`, `n_state_update1!`/`n_state_update_dyn_patch!`, `n_state_update2!/2h!/2g!`, `n_state_update3!`/`n_state_update_leaching!`, `cn_precision_control!` + `truncate_*`. Every fn: per-col/per-patch kernels (in-thread j/k/l loops, own-index — NO scatter), device-view `@adapt_structure` bundles for field-heavy patch loops, eltype-converted literals, `MetalF32` Float32-down-convert harness. Each gated: full suite 15681/15681 + `--check-bounds` + its own `scripts/gpu_validate_*_e2e.jl` Metal harness. **BGC-harness gotchas that ONLY the Metal harness caught (CPU-green missed both):** (1) crit-threshold scalars passed Float64 → invalid Metal IR; convert to `eltype(state)` at the launch site. (2) a host-built Int index vector (`filter_truncatep`) moved to device via `adapt(typeof(float_ref), …)` coerces indices to Float32 → can't index device arrays; use `similar(ref, Int, n)`+`copyto!` to preserve Int eltype. |
| Decomposition & soil cycling | 8 | ~200 | ✅ **DONE — all 8 files whole-fn on Metal at parity** | Parallel workflow batch (7 disjoint-file agents). **DONE (whole-fn Metal at 0.0/round-off):** `decomp.jl` (`soil_biogeochem_decomp!` application), `decomp_competition.jl` (`soil_bgc_competition!`), `decomp_vertical_profile.jl` (`soil_bgc_vertical_profile!` — the TRUE patch→column scatter via `_scatter_add!`), `decomp_precision_control.jl` (`soil_bgc_precision_control!`). **ALSO DONE (whole-fn Metal, hands-on after the batch):** `decomp_bgc.jl` (`decomp_rate_constants_bgc!`) + `decomp_potential.jl` (`soil_bgc_potential!`) — completing the DEFAULT (BGC/century) decomposition chain on GPU — and `decomp_mimics.jl` (`decomp_rates_mimics!`, non-default MIMICS). These needed Metal-hardening of the PRE-EXISTING `_decompb_*`/`_decpot_*`/`_mimics_*` kernels the agents left untouched: eltype-convert every Float64 literal in-kernel + convert scalar args (Q10/tfrz/minpsi/maxpsi/mino2lim/texture-params/crit-thresholds) at the launch site. Banked device-only bugs (CPU-green-but-Metal-broken): host scratch via `similar(state)` not `zeros()`; geo/ones() vectors via `similar(state)`; move `floating_cn_ratio`/`initial_cn_ratio`/cascade-pool vectors to the state backend (preserve Bool/Int eltype); `DecompBGCState`/`DecompMIMICSState` reparam'd loose-`{…,M<:AbstractMatrix}` + `@adapt_structure` so device arrays adapt in; a Float32-build harness must build the CPU reference UNIFORMLY at one precision (init_decompcascade_mimics! allocates texture arrays at Float64 regardless → build CPU ref all-Float64, down-convert to device via MetalF32). **LESSON: a partially-kernelized module's pre-existing kernels are NOT Metal-safe — they predate the requirement; budget a literal/scalar-hardening pass per kernel.** **`litter_vert_transp.jl` (tridiag) DONE — the layer-coupled advection-diffusion solve is two per-column kernels: a coefficient kernel + a per-(tracer,pool) transport kernel with an IN-THREAD Thomas solve inlined to match `tridiagonal_solve!` exactly (no singularity guard, unlike `tridiagonal_multi!`) for byte-identity; 15-matrix `_LvtScr` scratch bundle, eltype-generic `patankar_A`, node-geometry vectors moved to the state backend. Metal harness 18/18 (cryoturb/bioturb/spinup).** Decomposition module COMPLETE (8/8). |
| Methane (CH4) | 1 | ~80 | **✅ COMPLETE — entire ch4Mod (all 7 fns) whole-fn on Metal at parity** | Foundation: `CH4Data` reparam'd `{FT,V,M,Vb}` + `@adapt_structure` (74 fields). DONE (Metal-validated): `get_jwt!`/`ch4_annualupdate!`/`ch4_totcolch4!` (diag, reductions), `ch4_oxid!`, `ch4_prod!` (per-patch rr_vr `_scatter_add!`), `ch4_aere!` (patch→col scatter), **`ch4_tran!`** — the 2-species CH4/O2 reaction-diffusion vertical transport, the hardest fn in the module: ONE per-column kernel (competition + ebul/aere reductions + 2-species source/epsilon + jwt-branchy Patankar tridiag assembly + IN-THREAD Thomas solve ×2, inlined to match `tridiagonal_solve!`), `_Ch4TS`/`_Ch4TF`/`_Ch4Scr`/`_Ch4TP` device-view bundles, and **`ch4!` orchestrator** — ~11 `_ch4o_*` per-column/per-(c,j) kernels + the grnd_ch4_cond patch→col and column→gridcell flux scatters (`_scatter_add!`), with the sat/lake dispatch (calls the 6 device kernels) + conservation `@warn` checks host-side (materialized once); non-default lake path stays host-verbatim. Pre-existing `meth_khcc!`/`meth_ebul!` kernels Metal-hardened (literals + scalar args). Done via a mix: foundation+`ch4_tran!`+orchestrator hands-on, the 4 mid-tier fns via a parallel workflow over disjoint methane.jl line-regions (cherry-picked). Gates: full suite 15681/15681 + --check-bounds 15681/15681 + 7 `gpu_validate_ch4*_e2e.jl` Metal harnesses at parity (whole ch4!: all 10 outputs ≤2.4e-7 F32). |
| Phenology | 2 | ~25 | **✅ COMPLETE — whole cn_phenology! on Metal at parity** | The 4 remaining host fns kernelized via a parallel Workflow batch (crop_phenology!, cn_season_decid_phenology! + cn_stress_decid_phenology! FSMs, cn_litter_to_column! 3-level patch→col scatter + new 3D `_scatter_add!`), then Metal-hardened (2 more agent batches, read-only returns): the 2 FSMs + evergreen + onset_growth grouped into device-view structs (`_PhenSeasonSD*`/`_PhenStressSD*`/`_PhenEvergreen*`/`_PhenOnset*` + isbits scalar bundles, @adapt_structure) past the 31-arg limit; 8 pre-existing kernels had Float64 literals → zero(T)/one(T)/T(x); wrapper scalars T()-converted. **PftConPhenology reparam'd `{FT,V,M,VI,VB}` + @adapt_structure** (was concrete Vector{Float64} → couldn't move to device — the one type blocking the whole-fn run). All mask sigs widened BitVector→AbstractVector{Bool}. Gate: full suite 15681/15681 + --check-bounds 15681/15681 + gpu_validate_phenology_e2e.jl (whole cn_phenology! phase 1+2, 4 PFT paths: evergreen/season-decid/stress-decid/crop): all 21 outputs ≤1.1e-7 F32. satellite_phenology.jl (prescribed-LAI, non-use_cn) left host — separate path. |
| Fire & disturbance | 2 | ~31 | **✅ COMPLETE — both fire orchestrators whole-fn on Metal at parity** | The whole fire module (fire_base + fire_li2014) is on GPU. 4 host fns kernelized via a parallel Workflow batch (p2c! patch→col scatter-reduce + new convention; cnfire_calc_fire_root_wetness_li2014!/li2021! per-patch + in-thread j-accum, soil_suction inlined; cnfire_fluxes! → 4 kernels incl. the (j,p) litter/CWD patch→col scatter; cnfire_area_li2014! → 12 kernels: the burned-area driver's scatter sections + per-column fire-spread). Metal-hardened via 2 read-only agent batches: the 4 big kernels (patchflux ~107 args, fire_spread ~60, litterscatter ~56, noncrop_main ~39) grouped into device-view structs (_FireC/NFireOut/_FireC/NLitOut/_FireStateC/N/_FirePft/_FireSpread* + isbits scalar bundles, @adapt_structure); 17 smaller kernels literal-hardened. **5 structs reparam'd** {FT,…}+@adapt_structure (PftConFireBase/Li2014, CNFireBaseData, CNFireLi2014Data, DgvsFireData). Hands-on Metal tail: widen all sigs to Abstract*; device-resident scratch via similar(); mask_actfire{c,p}=similar(mask,Bool,…) (BitVector on CPU per tests, device Bool on GPU); is_litter/is_cwd copied onto backend (DecompCascadeConData pins ::BitVector); copy dzsoi_decomp onto backend; T()-convert ~9 launch-site scalars. decomp_cascade_con stays host in the AREA path (i_cwd host loop). Gates: full suite 15681/15681 + --check-bounds + gpu_validate_fire_area_e2e.jl (14 burned-area outputs ≤6.8e-8) + gpu_validate_fire_fluxes_e2e.jl (14 C/N flux outputs ≤1.4e-10), both whole-fn on Metal F32. |
| Nitrogen cycling (nitrif_denitrif/n_dynamics/n_leaching) | 3 | ~20 | **✅ COMPLETE — all 3 orchestrators whole-fn on Metal at parity** | Workflow batch (3 worktree agents, oracles 175/72/77): nitrif_denitrif! (one per-column kernel + internal j-loop, exp/atan/sqrt/pow), n_fert!/n_soyfix! (2 p→col `_scatter_add!` finishing n_dynamics), n_leaching! (5 kernels: per-column level reductions tot_water/surface_water in-thread + per-(c,j) leaching/runoff, both config modes). Metal-hardened via a read-only agent batch: `_nitrifdenit_kernel!` (~62 args) grouped into `_NitrifOut{M}`/`_NitrifIn{M}`/`_NitrifScalars{T}`; the other ~8 kernels literal-hardened (4 already safe). **The N structs were ALREADY parametric+@adapt_structure'd — no struct reparam needed.** Metal tail: device-resident n_leaching scratch via similar(); widen n_dynamics kwarg sigs. Gates: full suite 15681/15681 + --check-bounds + gpu_validate_{nitrif_denitrif,n_leaching,n_dynamics}_e2e.jl — nitrif (both use_lch4) ≤9e-8, leaching (both modes) ≤4e-15, n_dynamics (deposition/fixation/fert/soyfix) ≤3e-15, all whole-fn Metal F32. HARNESS lesson: CropData pins ::FT scalars (need F32) but the Soil/CNVeg-N structs pin concrete ::Float64 (must stay F64) → use TWO down-convert adaptors. |
| Tail batch 1 (default-path veg/CN flux) | 5 | ~25 | **✅ kernelized + Metal-hardened; 2 of 5 whole-fn Metal e2e** | Workflow batch (5 worktree agents, oracles 44/31/50/54/72): cn_gresp! (growth resp, per-patch), cn_mresp! (maint resp: tcsoi per-(c,j) + per-patch MR + froot_mr in-thread j-accum, 3 kernels), cn_gap_mortality! + cn_gap_patch_to_column! (per-patch + 3-level patch→col scatter), calc_gpp_mr_availc! + calc_allometry! (allocation; calc_crop_allocation_fractions! DEFERRED — error()+crop_phase!), cn_annual_update! (per-col+per-patch). Metal-hardened via read-only agent batch: 4 big kernels (gapmort_patch ~92 args, gresp ~51, gap_p2c ~57 scatter, gpp ~44) grouped into device-view structs; rest literal-hardened. **6 PFT/param structs reparam'd {FT,V(,M)}+@adapt_structure** (PftConGrowthResp/MaintResp/Allocation/GapMort, DgvsGapMortData, GapMortalityParams — were concrete Vector/Matrix{Float64}). Metal e2e: gpu_validate_growth_resp_e2e.jl (cn_gresp! incl. [p,k] matrix grouping) ≤9e-9 + gpu_validate_gap_mortality_e2e.jl (grouping + 3D scatter) ≤3.6e-15. Full suite 15681/15681 + --check-bounds. maint/gpp/allometry/annual: hardened + CPU-gated (patterns = gresp grouping + n_leaching reductions, proven on Metal); per-fn e2e pending. |
| Tail wave 2a (CN allocation pipeline) | 3 | ~15 | **✅ kernelized + Metal-hardened; cnalloc whole-fn Metal e2e** | Workflow batch (3 worktree agents, oracles 73/73/54): calc_plant_cn_alloc! (per-patch C/N alloc, 2 kernels incl. optional C13/C14 in a flag-gated separate kernel), calc_plant_nitrogen_demand! (5 kernels: N demand + per-patch tempsum/tempmax max/min — NOT a reduction — + crop grain-fill state machine keeping the crop_phase! host call + retransn), calc_crop_allocation_fractions! (per-patch 3-phase crop alloc; **error() HOISTED to a host pre-check loop** so the kernel is error-free). Metal-hardened: _cnalloc_main_kernel! (~69 args) grouped into _CnAllocOut{V,M}/_CnAllocIn{V,M,VB} (croplive Bool → own VB param); 2 others literal-hardened (5 were already safe). PftConNutrientCompetition reparam'd {FT,V}. gpu_validate_nutrient_comp_e2e.jl: calc_plant_cn_alloc! ≤7e-8 on Metal. Full suite 15681/15681 + --check-bounds. ndemand/crop_fractions: hardened + CPU-gated (per-patch + crop_phase!-call pattern; per-fn e2e pending). |
| satellite_phenology (prescribed LAI / non-CN path) | 3 | ~6 | **✅ COMPLETE — whole module on Metal at parity** | Kernelized hands-on (T()-literals from the start, no separate hardening pass): satellite_phenology! (per-patch LAI/SAI/height interp + snow-burial branch on itype), read_annual_vegetation! (per-patch + internal k=1:12), read_monthly_vegetation! (per-patch + k=1:2 + mlaidiff fold). Reparam'd SatellitePhenologyData {FT,V,M}+@adapt_structure (was single-FT param w/ concrete Vector{Float64} timwt + Matrix{FT} → adapt no-op). gpu_validate_satellite_phenology_e2e.jl: all 9 outputs ≤2.2e-8 on Metal. Oracle 287/287; suite 15681/15681 + --check-bounds. |
| cn_products + p2g_1d! (wood/crop product pools) | 3 | ~10 | **✅ COMPLETE — partition fns whole-fn on Metal at parity** | Kernelized p2g_1d!'s unity-scale fast path (subgrid_ave.jl: all-3-scales-unity → 3 kernels zero/`_scatter_add!`-scatter-reduce/finalize-SPVAL; general urban-scale path stays host) — unblocks cn_products. cn_products_partition_wood_fluxes! + partition_crop_fluxes!: per-fp filter kernels + the unity p2g_1d! + dwt patch→gridcell `_scatter_add!`; the misconfiguration error() guard kept as a HOST-only pre-check (`if arr isa Array`) so CPU keeps the @test_throws while device skips it for valid data. CNProductsFullData reparam'd {FT,V}. gpu_validate_cn_products_e2e.jl: all 9 outputs (gru/hrv patch+grc, dwt scatter, crop) ≤4.6e-8 on Metal. Oracle 92/92; suite 15681/15681 + --check-bounds. |
| c_iso_flux (CNCIsoFluxMod, use_c13/c14) | ~12 | ~20 | **✅ COMPLETE — whole module on Metal at parity** | The 12 remaining per-patch host loops in c_iso_flux1!/2!/2h!/2g!/3! kernelized (crop harvestc init/add-grain/add-struct, reproductive MR, the fire CWD/litter splits, and the patch→column scatters litter/gap/harvest/gross-unrep). 9 kernels were Metal-safe as-is; the 3 oversized scatter kernels grouped into device-view bundles via a Workflow hardening batch: `_ciso_flux3_litrfire` (3 bundles, inline-launch-block form inside c_iso_flux3!), `_ciso_gap_p2c` (9 bundles, full `cn_c_iso_gap_pft_to_column!` wrapper), `_ciso_harvest_p2c` (6 bundles; agent output was truncated at the wood-harvest launch → grafted the original loose `_ciso_harvest_wood_p2c` 1D-scatter tail back on). gpu_validate_c_iso_flux_e2e.jl: gap + harvest (incl. wood) scatters ≤4.6e-8 on Metal. Oracle 45/45; suite green + --check-bounds. NOTE: c_iso_flux_calc! leaf wrapper still pins mask::BitVector (not device-dispatchable) — the kernel itself is fine, only the outer wrapper signature would need widening if ever called on-device. |
| cn_balance_check (CNBalanceCheckMod) | 6 | ~10 | **✅ COMPLETE — whole module on Metal at parity** | All 6 fns kernelized: begin_cn_gridcell/column_balance (per-gridcell/column), c2g_unity! (per-column→gridcell scatter = zero-kernel + `_scatter_add!`-kernel), c_balance_check! (column + gridcell numeric kernels), n_balance_check! (column kernel grouping its 21 input arrays into a `_NBalColIn{V}` device-view bundle to clear Metal's arg limit + gridcell kernel). PATTERN: each check loop split into (a) a per-element numeric kernel that computes the err/end arrays on device+host, and (b) a HOST-ONLY warn/error scan gated by `if errarr isa Array` — the CPU keeps the `@test_throws ErrorException` (2 of them) + @warns while the device just computes numbers (the error() builds a String from ~15 arrays, unbuildable from device scalars). CNBalanceData reparam'd {FT,V}+@adapt_structure (4 threshold scalars stay concrete ::Float64, host-only). `dt` down-converted `FT(dt)` at every launch (Metal rejects Float64 scalar args). Device-resident scratch (col_errcb/grc_errcb/col_ninputs…) via `fill!(similar(bal.begnb_col,FT,n),zero)`. gpu_validate_cn_balance_check_e2e.jl: all 10 outputs (beg/end cb/nb col+grc, c2g totc/totn_grc) rel=0 on Metal F32. Oracle 46/46 incl 2 @test_throws; suite + --check-bounds. NOTE: module is a placeholder in the default driver (not yet wired into clm_drv!) — kernelized for eventual GPU use + oracle parity. |
| Deferred per-fn e2e (maint/gpp/allometry/annual/ndemand/crop_fractions) | 6 (+p2c helper) | ~12 | **✅ COMPLETE — all 6 now whole-fn on Metal at parity** | These were "hardened" (kernels written) but had host scratch / host tails forcing the CPU backend. Fixes: (1) cn_mresp! tcsoi `Matrix{FT}(undef,…)`→`similar(t_soisno,…)` + masks `BitVector`→`AbstractVector{Bool}`; (2) calc_plant_nitrogen_demand! crop_phase_vals `zeros(…)`→`similar(…)`; (3) calc_crop_allocation_fractions! — widened `crop_phase!`'s `crop_phase_out::Vector`→`AbstractVector` (phenology.jl) + gated the crop-phase validation host loop with `if crop_phase_out isa Array`; (4) cn_annual_update! — end_of_year `fill(false,…)`→`similar(col.is_fates,Bool,…)` + **kernelized `p2c_1d_filter!`** (subgrid_ave.jl: per-column gather, also speeds the 11 biogeophys-driver call sites) + split the any_vegp/p2c tail: CPU keeps the gated BitVector-mask logic byte-for-byte, device always-launches the end_of_year-masked p2c (no-op when nothing is at EoY). calc_gpp_mr_availc! + calc_allometry! needed no source change. Harnesses: gpu_validate_alloc_pipeline_e2e.jl (14 outputs ≤2.6e-9), gpu_validate_maint_resp_e2e.jl (≤6e-13), gpu_validate_ndemand_e2e.jl (≤5.4e-9), gpu_validate_cn_annual_update_e2e.jl (10 outputs incl p2c rel=0). LESSON: the scoping Explore agent wrongly called 4 of these "ready" — crop_phase! pinned ::Vector and crop_phase_vals/end_of_year were host scratch; always re-verify "ready" claims by actually launching on Metal. |
| voc_emission (VOCEmissionMod, MEGAN) | 1 (+6 helpers) | ~25 | **✅ COMPLETE — whole module on Metal at parity** | The per-patch loop (calling 6 get_gamma_* helpers + get_map_EF, indexing `Vector{MEGANCompound/MechComp}` structs with String/nested-vector fields) is now one Metal kernel. (1) MEGANFactors reparam'd {FT,V}; (2) all 6 gamma helpers literal-hardened to `T=typeof(arg)` (byte-identical at Float64, Float32-safe — unit-tested directly); (3) compound/mechanism metadata FLATTENED to numeric arrays + moved to backend via `copyto!(similar(...),host)`; (4) per-patch `vmeg[p,imeg]` device scratch (n_megcomps is runtime); (5) kernel args grouped into `_VocOut`/`_VocMeg`/`_VocMech`/`_VocMF`/`_VocForce` device-view bundles. LESSONS: a *mutable* struct (MEGANFactors) is NOT isbits → can't be a kernel arg → repack into an immutable `_VocMF` bundle (loosened get_gamma_A's `mf::MEGANFactors`→`mf`); a Float64 *global* as a kwarg default (get_gamma_T tfrz=TFRZ) is a runtime double → pass `FT(TFRZ)` as a kernel scalar arg via keyword; helpers called positionally inside the kernel use default PFT-index kwargs (= voc_emission!'s defaults, the only config used). gpu_validate_voc_emission_e2e.jl: 11 outputs ≤6e-7 Metal F32. Oracle 108/108; suite + --check-bounds. (No-op in default driver — kernelized for eventual MEGAN use.) |
| cndv (CNDVEcosystemDynMod, use_cndv) | 4 of 6 | ~20 | **◐ WIRED + 4 of 6 fns on Metal at parity; establishment+driver remain** | cndv.jl + types/dgvs.jl wired into CLM.jl + runtests (test_cndv 70/70 in suite). DGVSData reparam'd {FT,V,VB} + DGVEcophysCon {V}. ON METAL: **cndv_light!** (2-pass light competition: per-patch crown/LAI/FPC kernel + patch→gridcell `_scatter_add!` of tree/shrub/grass totals + per-gridcell max kernel + per-patch competition kernel; the needed PftconType+eco fields extracted into a `_CndvLightPar` device bundle via copyto!+similar since PftconType is a 148-field non-parametric struct), **dyn_cndv_init!**, **dyn_cndv_interp!**, **cndv_update_acc_vars!** (simple per-patch kernels). gpu_validate_cndv_light_e2e.jl: 9 outputs ≤4e-8 Metal F32. NEW LESSON: **Metal has NO 64-bit atomics** → a `_scatter_add!` into an Int64 accumulator (numtrees) fails with `gpu_gc_pool_alloc`/`throw_methoderror`; make the counter **Int32** (`similar(...,Int32,...)` + `Int32(1)`). REMAINING (the hard part): **cndv_establishment!** has per-patch passes (parallelizable w/ scatters) BUT also a SEQUENTIAL `fpc_total[g]` adjustment pass (each patch reads the running gridcell total modified by earlier patches) → needs a one-thread-per-GRIDCELL kernel with an inner sequential patch loop (or per-gridcell thread-local accumulators); **cndv_driver!** (3 trivial per-patch loops climate20/filter/reset) is whole-fn-Metal only once establishment is. |

## Phase C — Reverse-mode AD on GPU (item #3) — HARDWARE-GATED

- Forward-mode (ForwardDiff) already verified on Metal through kernelized ops.
- Reverse-mode (Enzyme) works on CPU; does NOT run on Apple Metal (no Apple-GPU
  Enzyme support → hangs compiling the adjoint). Needs **CUDA/AMD hardware**.
- Also blocked on CPU full-driver by a Julia-1.12 codegen bug ("instruction does
  not dominate all uses") → revisit on a Julia LTS with the fix.
- **This session: write unvalidated scaffolding** (`scripts/gpu_ad_reverse_validate.jl`)
  ready to run when a CUDA box + fixed Julia are available. Cannot be validated here.

---

### Rough effort
Phase A: ~15-20 driver functions, each a focused session (A2/A3 fast via canopy
reuse; A7/A8 hardest). Phase B: many sessions (it's ~half the total loop volume).
Phase C: blocked — scaffolding only until hardware.

### Recommended immediate sequence
~~A1 → A2 → A3 → A4 → A5 → A6 → A7 → A8~~ DONE (all merged to main at 15681/15681).
Phase A COMPLETE — the entire biogeophys/hydrology driver (A1–A10) runs whole-function
on Metal at 0.0/round-off parity, full suite 15681/15681 byte-identical, every function
gated on its own gpu_validate_*_e2e.jl harness. Last piece (urban_fluxes! iterative
canyon-air solve) landed. Remaining HOST in the driver: only the hillslope-connectivity
path of lateral flow (intentionally CPU; device asserts non-hillslope). Next: Phase B
(BGC / use_cn) — the larger remaining body of work. The soil_hydrology.jl tail
(drainage! + clm_vic_map!, perched/subsurface lateral flow, perched/theta water-table
variants, irrigation withdrawals + urban ponding) is now DONE — all whole-fn on Metal
at 0.0/round-off, validated post-merge. Phase C scaffolding can be written anytime
(no validation feedback). Phase B after Phase A biogeophys is GPU-complete.
