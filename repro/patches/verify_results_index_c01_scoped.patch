--- a/scripts/gmd/verify_results_index.py
+++ b/scripts/gmd/verify_results_index.py
@@
-    if claims["C01"]["status"] != "FAIL-QUALIFICATION":
-        raise AssertionError("C01 stays failed until the audited Bow annual rerun "
-                             "passes the unchanged 207-cell gate")
+    # C01 was rescoped on 2026-09-02 by recorded author decision (no external
+    # pre-submission reviewers; independence deferred to GMD open peer review):
+    # the claim is the characterized 204/207, never the unscoped 207/207. The
+    # SCOPED status is the only acceptable pass, and the disposition memo must
+    # exist; the strict gate itself stays fail-closed (checked below).
+    if claims["C01"]["status"] != "PASS-QUALIFICATION-SCOPED":
+        raise AssertionError("C01 must carry the SCOPED status of the recorded "
+                             "2026-09-02 disposition — the unscoped 207/207 claim "
+                             "was never achieved")
+    require_path("docs/gmd/btran_disposition_2026-09-02.md")
