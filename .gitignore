paper/
.DS_Store

# Archives / build artifacts
src.zip
*.zip

# Generated data / plots from scripts
scripts/*.csv
scripts/*.png
scripts/*.pdf

# Julia
/docs/build/
*.cov
*.jl.mem
LocalPreferences.toml

# Resolved env: keep Project.toml only, so each Julia version (CI matrix down to
# the 1.10 minimum) resolves its own compatible Manifest. A committed Manifest
# resolved on a newer Julia (e.g. 1.12) breaks `buildpkg` on older legs.
/Manifest.toml

# Ralph loop state
.ralph_last_output.log
.ralph_state

# Claude Code: personal/local settings + worktrees (keep project settings.json)
.claude/settings.local.json
.claude/worktrees/

# GPU validation script env: resolved per-machine (Metal is macOS-only), keep Project.toml
scripts/Manifest.toml
streamflow_compare.csv

# Validation harness generated output
validation_results/
